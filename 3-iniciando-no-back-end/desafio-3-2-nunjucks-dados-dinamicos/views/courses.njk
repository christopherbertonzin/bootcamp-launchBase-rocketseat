{% extends 'layout.njk' %}

{% block content %}

    <section class="content">

        {% for item in items %}

        <div class="card {{item.class}}">
            <span class="span ">{{item.id}}</span>
            <img src="./assets/images/{{item.img}}" alt="logotipo e título" class="card__logo">
            <p class="card__"><strong>{{item.strong}}</strong>{{item.description}}</p>
        </div>

        {% endfor %}

    </section>
    
    {# <div class="modalOverlay">
        <div class="modal">

        </div>
    </div>
     #}
    <script src="./scripts.js"></script>

{% endblock content %}

